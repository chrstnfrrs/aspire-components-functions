/*
 * set color to themed color
 * if (document && getComputedStyle(document.documentElement).getPropertyValue(type)) {
 *   styles[color]: getComputedStyle(document.documentElement).getPropertyValue(type);
 * }
 */
